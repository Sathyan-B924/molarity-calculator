/*
 * Compound presets are intentionally empty in version 1.
 * Future presets can be added as objects such as:
 * { name: "Sodium borohydride", formula: "NaBH4", molarMass: 37.83 }
 */
globalThis.MOLARITY_COMPOUNDS = Object.freeze([]);
